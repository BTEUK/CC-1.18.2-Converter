name: Converter
version: '${project.version}'
main: me.bteuk.network.Network
api-version: 1.18

commands:
  convert:
    usage: /<command>
    description: Converts all or some of the json files into the game.